function toggleMenu(element) {
    element.classList.toggle("menu-open");
    document.getElementById("sidebar").classList.toggle("show-sidebar");
    document.getElementById("sidebar-cover").classList.toggle("show-sidebar");
}